services:
  vue-capacitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vue-capacitor
    working_dir: /app
    stdin_open: true
    tty: true
    volumes:
      - ./app:/app
